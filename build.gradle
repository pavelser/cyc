ext{
    childProcesses = [:]
}

allprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
}

configure(subprojects) {
    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}

repositories {
    mavenCentral()
}